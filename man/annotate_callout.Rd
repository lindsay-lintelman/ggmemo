% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-callout.R
\name{annotate_callout}
\alias{annotate_callout}
\title{Add a callout annotation to a ggplot}
\usage{
annotate_callout(data, where, label, position = "top-right", nudge = NULL)
}
\arguments{
\item{data}{A data frame. Should be the same data frame used in the
ggplot, or a subset of it. Must contain the columns mapped to x and y
in the plot's \code{aes()}. Note: the automatic nudge heuristic estimates
label offset from the data ranges, but it guesses which columns are
x and y. For data frames with many numeric columns, passing a
two-column subset (e.g., \code{data[, c("date", "sales")]}) or setting
\code{nudge} explicitly gives more reliable placement.}

\item{where}{<\link[rlang:args_data_masking]{tidy-eval}> A filtering
expression that identifies exactly one row of \code{data}. For example,
\code{year == 2020} or \code{quarter == "Q4" & region == "West"}. An error is
thrown if the expression matches zero or more than one row.}

\item{label}{A single character string for the annotation text.}

\item{position}{Where to place the label relative to the data point.
One of \code{"top-right"} (default), \code{"top-left"}, \code{"bottom-right"}, or
\code{"bottom-left"}.}

\item{nudge}{Optional numeric vector of length 2 (\code{c(x, y)}) giving
explicit nudge amounts in data units. Overrides the automatic nudge
heuristic, which estimates 5\% of the x and y data ranges. The
heuristic works well when \code{data} contains only the plotted columns;
if \code{data} has many numeric columns (like \link[ggplot2:economics]{ggplot2::economics}),
passing a two-column subset or setting \code{nudge} explicitly avoids
the heuristic picking the wrong column's range.}
}
\value{
A ggplot2 layer that can be added to a plot with \code{+}.
}
\description{
Points at a specific data row with an arrow and label. The callout
consists of a text label inside a rounded box, connected to the target
data point by a line segment with an arrowhead. Built on top of
\code{\link[ggpp:geom_label_s]{ggpp::geom_label_s()}}.
}
\examples{
library(ggplot2)

p <- ggplot(economics, aes(x = date, y = unemploy)) +
  geom_line()

# Basic callout
p + annotate_callout(
  economics,
  where = date == as.Date("2009-10-01"),
  label = "Peak unemployment",
  position = "top-right"
)

# With explicit nudge (useful when data has many numeric columns)
p + annotate_callout(
  economics,
  where = date == as.Date("2009-10-01"),
  label = "Peak unemployment",
  nudge = c(365, 500)
)

}
\seealso{
\code{\link[=annotate_change]{annotate_change()}} to label the delta between two data points.
}
