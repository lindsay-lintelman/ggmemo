% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-change.R
\name{annotate_change}
\alias{annotate_change}
\title{Annotate the change between two data points on a ggplot}
\usage{
annotate_change(
  data,
  from,
  to,
  value,
  format = "percent",
  colors = c(up = "#2E7D32", down = "#B22222", flat = "#808080"),
  ...
)
}
\arguments{
\item{data}{A data frame. Should be the same data frame used in the
ggplot. Must contain the columns mapped to x and y in the plot's
\code{aes()}, as well as the column specified in \code{value}.}

\item{from}{<\link[rlang:args_data_masking]{tidy-eval}> A filtering
expression that identifies exactly one row of \code{data} for the start
of the arrow. For example, \code{quarter == "Q2"}. An error is thrown if
the expression matches zero or more than one row.}

\item{to}{<\link[rlang:args_data_masking]{tidy-eval}> A filtering
expression that identifies exactly one row of \code{data} for the end
of the arrow.}

\item{value}{<\link[rlang:args_data_masking]{tidy-eval}> An unquoted
column name indicating which numeric column to compute the change
on. For example, \code{value = revenue}.}

\item{format}{How to format the delta label. One of \code{"percent"}
(default), \code{"absolute"}, \code{"points"}, or \code{"both"}. Percent change
from a zero base value falls back to absolute with a warning.
Percent change from negative values uses the raw formula and may
be confusing; use \code{"absolute"} for data that can go negative.
Use \code{"points"} when the data is already a rate or percentage
(e.g., savings rate, market share) — it labels the difference
in percentage points (e.g., "+9.8 \%pts") instead of computing
a misleading percent-of-percent.}

\item{colors}{Named character vector of length 3 with hex color values
for the arrow and label. Names must be \code{"up"}, \code{"down"}, and \code{"flat"}.
Defaults to dark green, dark red, and grey.}

\item{...}{Additional arguments passed to the label layer
(\code{\link[ggplot2:annotate]{ggplot2::annotate()}} with \code{geom = "label"}). Use to override
defaults like \code{size}, \code{fontface}, or \code{fill}.}
}
\value{
A list of ggplot2 layers (arrow + label) that can be added
to a plot with \code{+}.
}
\description{
Draws a straight arrow between two data rows and labels the midpoint
with the computed delta. The label is color-coded: dark green for
increases, dark red for decreases, grey for no change. Built on top of
\code{\link[ggplot2:annotate]{ggplot2::annotate()}}.
}
\examples{
library(ggplot2)

revenue <- data.frame(
  quarter = factor(c("Q1", "Q2", "Q3", "Q4"),
                   levels = c("Q1", "Q2", "Q3", "Q4")),
  revenue = c(120, 145, 132, 158)
)

# Percent change (default)
ggplot(revenue, aes(x = quarter, y = revenue)) +
  geom_col(fill = "grey70", width = 0.6) +
  annotate_change(
    revenue,
    from = quarter == "Q1",
    to = quarter == "Q4",
    value = revenue
  )

# Absolute change
ggplot(revenue, aes(x = quarter, y = revenue)) +
  geom_col(fill = "grey70", width = 0.6) +
  annotate_change(
    revenue,
    from = quarter == "Q1",
    to = quarter == "Q4",
    value = revenue,
    format = "absolute"
  )

# Percentage points (for data already expressed as rates)
rates <- data.frame(
  year = 2020:2023,
  rate = c(3.5, 8.1, 5.4, 3.7)
)
ggplot(rates, aes(x = year, y = rate)) +
  geom_line() +
  geom_point() +
  annotate_change(rates, from = year == 2020, to = year == 2021,
                  value = rate, format = "points")

# Custom colors (e.g., corporate palette)
ggplot(revenue, aes(x = quarter, y = revenue)) +
  geom_col(fill = "grey70", width = 0.6) +
  annotate_change(
    revenue,
    from = quarter == "Q1",
    to = quarter == "Q4",
    value = revenue,
    colors = c(up = "#1B9E77", down = "#D95F02", flat = "#7570B3")
  )

# Date x-axis (time series) — use nudge on the callout for wide data
ggplot(economics, aes(x = date, y = psavert)) +
  geom_line() +
  annotate_change(
    economics,
    from = date == as.Date("2005-07-01"),
    to = date == as.Date("2012-12-01"),
    value = psavert,
    format = "points"
  )

# Showing a decline (red arrow, negative label)
ggplot(revenue, aes(x = quarter, y = revenue)) +
  geom_col(fill = "grey70", width = 0.6) +
  annotate_change(
    revenue,
    from = quarter == "Q2",
    to = quarter == "Q3",
    value = revenue
  )

# Combined with annotate_callout() on a time series
ggplot(economics, aes(x = date, y = psavert)) +
  geom_line() +
  annotate_callout(
    economics,
    where = date == as.Date("2005-07-01"),
    label = "All-time low",
    nudge = c(365, 1)
  ) +
  annotate_change(
    economics,
    from = date == as.Date("2005-07-01"),
    to = date == as.Date("2012-12-01"),
    value = psavert,
    format = "points"
  )

}
\seealso{
\code{\link[=annotate_callout]{annotate_callout()}} to label a single data point.
}
